[pytest]
filterwarnings =
    ignore::DeprecationWarning:pygame.pkgdata
markers =
    initialization: Tests for object initialization
    movement: Tests for movement logic
    mode: Tests for mode switching
    collision: Tests for collision detection
    sprite: Tests for sprite rendering
    destination: Tests for destination updates